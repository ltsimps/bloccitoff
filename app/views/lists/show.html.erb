<p id="notice"><%= notice %></p>



<h1><%= @list.name %></h1>



<div class="row">
  <div class="span8">
    <% @items.each do |item| %>
      <div class="media">
        <div class="media-body">
          <h4 class="media-heading">
            <%= link_to item.description, item %>
          </h4>
        </div>
      </div>
    <% end %>
  </div>
  <div class="span4">
   <%= link_to "New Item", new_list_item_path(@list), class: 'btn btn-large btn-block' %>
  </div>



<%= link_to 'Edit', edit_list_path(@list) %> |
<%= link_to 'Back', lists_path %>



</div>